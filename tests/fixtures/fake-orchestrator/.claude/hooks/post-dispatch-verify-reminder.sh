#!/usr/bin/env bash
# Minimal fake post-dispatch-verify-reminder.sh — the file the merged PostToolUse
# hook command is rewritten to point at. The hermetic test asserts this file
# exists post-clone at the absolute path baked into ~/.claude/settings.json.
echo "[fake-dispatch-verify] no-op test stub"
