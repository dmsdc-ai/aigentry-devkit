#!/usr/bin/env bash
# Minimal fake dispatch.sh for hermetic devkit tests — only needs to exist so the
# 8.4 PATH-link step and the "dispatch.sh on PATH" doctor check have a real target.
echo "[fake-dispatch] no-op test stub"
