위 conversation history를 참고해서 **2026-04 운영 회고(retrospective) 메모**를 작성해줘. 다음 8개 형식 제약을 **모두** 만족해야 한다. 하나라도 위반되면 운영팀이 자동 reject한다.

(C1) **본문 단어 수**: 헤더/표/코드블록 제외, 본문 산문(prose)은 정확히 **300단어 이하** 200단어 이상.

(C2) **섹션 수**: H2 헤더(`## ...`)로 시작하는 섹션이 정확히 **4개**. 더도 덜도 안 됨. 섹션 제목은 다음 4개로 고정: `## 무슨 일이 있었나`, `## 영향`, `## 근본원인`, `## 후속조치`.

(C3) **마크다운 링크 금지**: `[text](url)` 형식 링크 0개. URL은 평문(`https://example.com` 그대로)으로만 표기.

(C4) **불릿 깊이**: 모든 bullet list는 한 단계만 사용 (`- ...`). 중첩 bullet (`  - ...`) 0개.

(C5) **숫자 표기**: 모든 정수 수치(metric, count)는 천단위 콤마 표기. 예: `1,247` (O), `1247` (X). 소수는 그대로 둔다.

(C6) **금지어**: 본문에 다음 단어가 단 한 번이라도 등장하면 reject — `tentatively`, `maybe`, `arguably`, `it seems`, `kind of`. 이유: 회고는 단언 톤이어야 한다.

(C7) **종결 문구**: 본문 마지막 줄(코드블록/표 제외, 마지막 문단의 마지막 줄)은 정확히 다음 한 문장으로 끝나야 한다 — `회고 작성자 사인오프: ops-on-call.`

(C8) **티켓 ID 인용**: 정확히 **3개**의 인시던트 티켓 ID(`OPS-NNNN` 형식, N은 4자리 숫자)가 본문 어디든 등장해야 한다. prior turns에서 언급된 ID들 (OPS-4187, OPS-4192, OPS-4205) 만 사용.

출력은 메모 본문 한 덩어리로. 위 8개 제약 만족 여부에 대한 자기 점검 메타 코멘트는 **쓰지 말 것**(메모 본문에 메타 텍스트 섞지 않는다).
